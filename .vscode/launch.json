{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Run Aircraft Black Box",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/blackbox",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build Aircraft Black Box"
        }
    ]
}